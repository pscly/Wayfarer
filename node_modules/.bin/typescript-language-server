#!/usr/bin/env sh

# Workspace-local shim for tools that expect `typescript-language-server` on PATH.
# Delegates to the web workspace dependency.

node "$(dirname "$0")/../../web/node_modules/typescript-language-server/lib/cli.mjs" "$@"
