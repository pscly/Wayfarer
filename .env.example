# Backend
# JWT signing keys are required for auth flows (production must set these).
# JSON map shape: kid -> secret (example only; NOT a real secret):
# {"dev-1":"not-a-real-secret-change-me"}
WAYFARER_JWT_SIGNING_KEYS_JSON=
# Current key id (must exist in WAYFARER_JWT_SIGNING_KEYS_JSON)
WAYFARER_JWT_KID_CURRENT=
WAYFARER_DB_URL=

# Migrations (docker entrypoint knobs). Missing migrations can cause INTERNAL_ERROR.
# 1/true to run migrations at start; 0/false to disable.
WAYFARER_MIGRATE_ON_START=
# 1/true to fail fast if migrations cannot run/apply.
WAYFARER_MIGRATE_STRICT=
# Retry DB readiness/migration attempts (e.g. 10).
WAYFARER_MIGRATE_MAX_ATTEMPTS=
WAYFARER_CELERY_EAGER=

# CORS / cookies (browser auth)
# Local dev origin: http://localhost:3000
# Production origin (waf.pscly.cc): https://waf.pscly.cc
WAYFARER_CORS_ALLOW_ORIGIN=
# Use 1/true when the browser must send cookies.
WAYFARER_CORS_ALLOW_CREDENTIALS=
# Cookie Secure flag: 0 for http dev, 1 for https (e.g. waf.pscly.cc)
WAYFARER_DEV_COOKIE_SECURE=

# Redis (optional). Documented support for either key; set one.
WAYFARER_REDIS_URL=
REDIS_URL=

# Web
# Local dev: http://localhost:8000
# Production (waf.pscly.cc): https://waf.pscly.cc
NEXT_PUBLIC_API_BASE_URL=
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=

# Third-party APIs
WAYFARER_AMAP_API_KEY=
